
V	?= 0 
CC      ?= gcc
CXX	?= g++
AR      ?= ar
ARFLAGS  = rcs
MKDIR   ?= mkdir -p
OPTFALGS ?= -O2 -ftree-vectorize -fvect-cost-model -march=native -DNDEBUG
#Disable inlining for coverage reports
COVFLAGS = -fno-inline-small-functions -fno-indirect-inlining -fno-partial-inlining --coverage
CFLAGS  = -MP -MD -Wall -pedantic -g -I. ${OPTFALGS}
CXXFLAGS = -MD -MP -Wall -pedantic -std=c++11 -g -I. ${OPTFLAGS}

BUILDDIR = .libs
PROGRAM  = dealer
PROGRAMCOV = ${PROGRAM}.cov
GENLIB = genlib

ifeq ($V, 1)
SCC    = $(CC)
SAR    = $(AR)
SCXX   = $(CXX)
SFLEX  = $(FLEX)
SYACC  = $(YACC)
SMKDIR = $(MKDIR)
else
SCC    = @echo "  CC    " ${notdir $@} && $(CC)
SAR    = @echo "  AR    " ${notdir $@} && $(AR)
SCXX   = @echo "  CXX   " ${notdir $@} && $(CXX)
SFLEX  = @echo "  FLEX  " ${notdir $@} && $(FLEX)
SYACC  = @echo "  YACC  " ${notdir $@} && $(YACC)
SMKDIR = @echo "  MKDIR " ${notdir $@} && $(MKDIR)
endif

